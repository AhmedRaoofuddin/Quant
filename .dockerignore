**/build
**/node_modules
**/.next
**/out
# Anchored to the context root, not **/data. The recursive form also excluded the C++
# backend/src/data and backend/include/alphaforge/data directories from the build context, so the
# image build could not find DataSource.hpp. Same class of bug as the .gitignore rule.
/data
frontend/.cache
**/test_tmp_data*
**/.git
**/.env*
**/*.exe
**/*.o
**/*.a
